
GIT INIT 	(se hace una sola vez para que sea un repositorio).

GIT ADD . 	(ponemos en estado de preparación todos los archivos).

GIT COMMIT -M "Mi primer commit" 	(pasar al repositorio local).

GIT REMOTE ADD ORIGIN "URL" 	(con esto subimos todo al git hub, la URL la sacamos de la cuenta del git hub). (así en realidad lo que hacemos es sincronizarlo).

GIT PUSH -U ORIGIN MASTER. 	( Ahora con esta instrucción se sube).




---------- CONFIGURAR VARIABLES GLOBALES ----------

git config --global user.name "nuevo nombre"

git config --global user.email "nuevo correo"

---------------------------------------------------

git diff (comparar diferencias)